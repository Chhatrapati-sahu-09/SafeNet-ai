# SafeNet AI environment template
# Copy to .env and fill with your own secrets if/when backend integration is added.

# Optional API endpoint for future classification service
SAFENET_API_BASE_URL=

# Optional API key for future model gateway
SAFENET_API_KEY=

# Optional provider keys (use only what your integration needs)
OPENAI_API_KEY=
GEMINI_API_KEY=
